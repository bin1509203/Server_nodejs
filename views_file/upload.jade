doctype html
html
    head
        meta(charset='utf-8')
    body
        form(action='/upload' method='post' enctype='multipart/form-data')
            input(type='file' name='userfile')
            input(type='submit')
            -// upload 라는 path로 사용자가 upload한 파일을 post 방식으로 전송
            -// 사용자가 전송한 파일을 서버로 저장하기 위한 옵션 = 'enctype'
            -// upload.single('userfile') 이거랑 form name 일치시켜야함.
